# This rule is not used by the project kube-greencosts itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants get/list/watch permissions over greencosts.hstr.nl.
# This role is intended for users authorized to view ClusterHibernatePolicy objects.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kube-greencosts
    app.kubernetes.io/managed-by: kustomize
  name: clusterhibernatepolicy-viewer-role
rules:
- apiGroups:
  - greencosts.hstr.nl
  resources:
  - clusterhibernatepolicies
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - greencosts.hstr.nl
  resources:
  - clusterhibernatepolicies/status
  verbs:
  - get
