---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: hibernatepolicies.greencosts.hstr.nl
spec:
  group: greencosts.hstr.nl
  names:
    kind: HibernatePolicy
    listKind: HibernatePolicyList
    plural: hibernatepolicies
    shortNames:
    - hp
    singular: hibernatepolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.hibernatedNamespaces
      name: Hibernated
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HibernatePolicy is the Schema for the hibernatepolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HibernatePolicySpec defines the desired state of HibernatePolicy.
            properties:
              action:
                description: Action defines what the controller does when idle conditions
                  are met.
                properties:
                  scaleDeploymentsToZero:
                    description: |-
                      ScaleDeploymentsToZero scales all Deployments in matching namespaces to
                      zero replicas. Original replica counts are preserved in an annotation.
                    type: boolean
                  snapshotPVCs:
                    description: |-
                      SnapshotPVCs creates a VolumeSnapshot for every PersistentVolumeClaim in
                      the namespace before scaling down.
                    type: boolean
                type: object
              idleDetection:
                description: |-
                  IdleDetection configures the conditions under which a namespace is
                  declared idle.
                properties:
                  cpuBelow:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      CPUBelow is the total CPU usage threshold below which the namespace is
                      considered idle (queried from the metrics-server).
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  ignoreDuring:
                    description: |-
                      IgnoreDuring lists time windows during which hibernation is suppressed.
                      Namespaces that were scaled down will be restored at the start of a window.
                    items:
                      description: IgnorePeriod defines a recurring time window during
                        which hibernation is suppressed.
                      properties:
                        from:
                          description: From is the wall-clock start time (HH:MM) in
                            Timezone.
                          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
                          type: string
                        timezone:
                          default: UTC
                          description: Timezone is the IANA timezone name for this
                            window (e.g. "Europe/Amsterdam").
                          type: string
                        until:
                          description: Until is the wall-clock end time (HH:MM) in
                            Timezone.
                          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
                          type: string
                        weekdays:
                          description: Weekdays lists the days of the week this window
                            applies to.
                          items:
                            description: Weekday is a three-letter abbreviation for
                              a day of the week.
                            enum:
                            - Mon
                            - Tue
                            - Wed
                            - Thu
                            - Fri
                            - Sat
                            - Sun
                            type: string
                          minItems: 1
                          type: array
                      required:
                      - from
                      - timezone
                      - until
                      - weekdays
                      type: object
                    type: array
                  networkBelow:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      NetworkBelow is the total network throughput threshold below which the
                      namespace is considered idle (queried from Prometheus).
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  noIngressRequestsFor:
                    description: |-
                      NoIngressRequestsFor is how long the namespace must have received zero
                      ingress HTTP requests before it is considered idle.
                    type: string
                type: object
              selector:
                description: Selector identifies which namespaces this policy governs.
                properties:
                  namespaceSelector:
                    description: NamespaceSelector selects namespaces by label expressions.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - namespaceSelector
                type: object
            required:
            - action
            - idleDetection
            - selector
            type: object
          status:
            description: HibernatePolicyStatus defines the observed state of HibernatePolicy.
            properties:
              conditions:
                description: Conditions reflect the current state of the HibernatePolicy
                  reconciliation.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              hibernatedNamespaces:
                description: |-
                  HibernatedNamespaces lists the namespaces currently scaled to zero by
                  this policy.
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
